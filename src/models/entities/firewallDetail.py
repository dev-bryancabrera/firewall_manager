class FirewallDetail:

    def __init__(
        self,
        id,
        rule_id,
        regla,
        estado,
    ) -> None:
        self.id = id
        self.rule_id = rule_id
        self.regla = regla
        self.estado = estado
